apiVersion: 1

providers:
  - name: 'smoke tests dashboard provider'
    folder: 'smoke tests'
    updateIntervalSeconds: 10
    allowUiUpdates: false
    options:
      path: "/etc/grafana/provisioning/dashboards"